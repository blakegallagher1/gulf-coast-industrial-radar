# Calcasieu Parish Assessor — Schema Research

> **Generated by** `source-schema-researcher` · model: sonar-deep-research · cost: $0.0276

## 1. URL Pattern

Calcasieu assessor property search:

```
https://www.calcasieuassessor.com/property-search
```

| Parameter | Type | Description |
|---|---|---|
| `name` | string | Owner name |
| `parcel` | string | Parcel number |
| `address` | string | Street address |
| `page` | integer | Page number |

```bash
curl 'https://www.calcasieuassessor.com/property-search?name=Cameron+LNG&page=1'
```

## 2. Response Format

HTML table `<table id="propertyTable">` with:

| Column | Description |
|---|---|
| Parcel No | Unique parcel ID |
| Owner Name | Current owner |
| Site Address | Property location |
| Land Value | Assessed land value |
| Improvement Value | Assessed improvement value |
| Total Value | Sum of land + improvements |
| Last Sale | Date and price of last sale |

## 3. Pagination

Query-string `page` parameter; 20 results per page.

## 4. Rate Limits

- No documented limit; 1 req/s safe.

## 5. Recent Changes

- **2024-Q4**: "Last Sale" split into separate "Sale Date" and "Sale Price" columns (our parser merges them back).
