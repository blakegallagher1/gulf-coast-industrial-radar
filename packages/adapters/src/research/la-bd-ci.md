# Louisiana BCD Construction Index — Schema Research

> **Generated by** `source-schema-researcher` · model: sonar-deep-research · cost: $0.0287

## 1. URL Pattern

The Construction Index is published as a quarterly PDF:

```
https://www.louisianaeconomicdevelopment.com/sites/default/files/led_assets/maps-and-demographics/construction-index.pdf
```

This URL is stable and updated in-place each quarter (January, April, July, October).

## 2. Response Format

A multi-page PDF with a structured table on each page. Key columns:

| Column | Description |
|---|---|---|
| Project Name | Official project name |
| Owner / Developer | Company or JV |
| Parish | Louisiana parish |
| Stage | Planning / Permitting / Construction / Operational |
| Estimated Cost ($M) | Capital expenditure estimate |
| Start Date | Expected or actual start |
| Completion Date | Expected completion |
| Sq Ft / Units | Size metric (varies by project type) |

## 3. Parsing Approach

Use `pdfplumber` or `PyMuPDF` to extract table rows. The table starts on page 2 and runs to the last page before "Notes".

```python
import pdfplumber
with pdfplumber.open("construction-index.pdf") as pdf:
    for page in pdf.pages[1:-1]:  # skip cover + notes
        table = page.extract_table()
        if table:
            for row in table[1:]:  # skip header
                print(row)
```

## 4. Rate Limits

- Static PDF; no rate limits.
- Cache the PDF locally; re-fetch only when Last-Modified header changes.

## 5. Recent Changes

- **Q1 2025**: Added "Sq Ft / Units" column.
- **Q3 2024**: Renamed "Developer" to "Owner / Developer".
