# USACE MVN Public Notices — Schema Research

> **Generated by** `source-schema-researcher` · model: sonar-deep-research · cost: $0.0312

## 1. URL Pattern

Public notices listing page (HTML):

```
https://www.mvn.usace.army.mil/Missions/Regulatory/Public-Notices/
```

No query parameters; the page lists all current public notices as HTML `<a>` links to individual PDF files.

```bash
curl 'https://www.mvn.usace.army.mil/Missions/Regulatory/Public-Notices/'
```

## 2. Response Format

The page renders an HTML table with columns:

| Column | Description |
|---|---|---|
| Notice Number | e.g. `MVN-2025-00123-EWW` |
| Applicant | Name of permit applicant |
| Location | Parish and coordinates |
| Comment Deadline | ISO date |
| PDF Link | Relative URL to the PDF |

Each row's PDF link points to the full permit application.

## 3. Pagination

No pagination; the page lists all open notices (typically 50–200). Archive notices are not listed.

## 4. Rate Limits

- Government site; no documented rate limit.
- robots.txt: disallows `/Portals/` (media storage) only.
- Recommend no more than 1 request per 2 seconds.

## 5. Recent Changes

- **2025-Q1**: Notice numbers now include district prefix (e.g. `MVN-` rather than bare numeric).
- **2024-Q3**: Added "Location" column with coordinates.

## 6. PDF Parsing

PDFs are unstructured. Key fields to extract via regex:

- `Public Notice Number:` → notice ID
- `Applicant:` → applicant name
- `Location:` → lat/long or parish
- `Work Description:` → project description
- `Comment Period Expires:` → deadline date
